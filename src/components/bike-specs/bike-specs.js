import React from 'react'
import SmoothScroll from '../smoothscroll/smoothscroll'

const BikeSpecs = (props) => {

    return (
        <SmoothScroll>
            <section className="bike-specs">
                <p>Bike Specs</p>
            </section>
        </SmoothScroll>
    )
}

export default BikeSpecs;

